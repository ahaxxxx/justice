{
  "name": "justice-cat",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install:frontend": "npm --prefix frontend install",
    "install:backend": "npm --prefix backend install",
    "dev": "npm run install:frontend && npm --prefix frontend run dev -- --host 127.0.0.1",
    "dev:backend": "npm run install:backend && npm --prefix backend run dev",
    "build": "npm --prefix frontend run build"
  }
}
